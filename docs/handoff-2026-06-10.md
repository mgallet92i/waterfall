# Handoff — campagne de durcissement ARCH (sessions 2026-06-09/10)

> Point de reprise. Décision HO (2026-06-10) : **rodage in vivo de cette version** avant
> tout nouveau chantier. ARCH-05, 07, 09 gardés au chaud.
> Source de vérité détaillée : [`../backlog.md`](../backlog.md). Remplace `handoff-2026-06-07.md`.

## Vérifier l'état sain à la reprise

```bash
bats tests/                       # attendu : 177 passed, 0 failed (~10 min, lent sous Windows)
bash -n scripts/wf-orchestrate.sh hooks/wf-auth.sh
```

⚠ **Leçon process** : toujours lancer la full suite APRÈS le dernier edit — une suite
démarrée avant des modifs lit les anciens fichiers et produit un faux vert (vécu sur ARCH-06 ét.2).

## Fait sur ces 2 sessions (tout poussé sur origin/develop)

| Lot | Contenu | Commit |
|---|---|---|
| Config | modèle `fable` pour les 8 rôles (+ allowlist `wf-read-config.sh`) | `5027ea2` |
| F-033 | artefact de revue unifié `review.md` (5 fichiers) | `a182775` |
| ARCH-06 ét.2 | purge des tables STEP_* des personas, agents hint-driven, drifts toxiques tués (`exit_decision=`, `BOOTSTRAP:INIT`, 5 steps fictifs), `tests/wf-doc-drift.bats` | `23aaab9` |
| F-034 | noms legacy `tf.md`/`tech.md`/`taches.md` → canoniques (16 fichiers) + garde CI | `e58de98` |
| Docs | `AGENTS.md` (guide dev) + `CLAUDE.md` racine + schémas mermaid par phase + garde CI nœuds | `44696d7`, `e9f8a88` |
| ARCH-03 B/C | verdict CODE_REVIEW (`code_review_verdict`) + routage DISPATCH déterministe (RV pose `has_functional`/`has_technical`, persistés) + branche morte `PO_UPDATE→TL_UPDATE` ressuscitée | `c9b2fc4` |
| ARCH-08 | ownership Write/Edit tous rôles (matrice `_wf_artifact_writers`), flat-deny Bash artefacts, canal `--append retro|tracking` step-gated | `a423c17` |

## ⚠ À faire avant le prochain run waterfall

- **`/reload-plugins`** (HO) — personas et constitution modifiés ; un agent spawné sans reload joue l'ancien contrat.
- Le run live EST le test : monitorer en particulier (a) RV pose-t-il bien verdict+routage à `RV_REVIEW`/`RV_CODE_REVIEW`, (b) OR utilise-t-il `--append` à `UPDATE_TRACKING*`/`LOG_AUDIT`, (c) aucun agent ne tente plus de heredoc sur artefact. Logger en `[OBS-NNN]`.

## Reste au backlog (gardé au chaud, ordre suggéré)

1. **ARCH-05** — combinatoire des modes : dériver UNE liste de steps effective par mode à l'init.
2. **ARCH-09** — idempotence mailbox (`processed_msg_ids`).
3. **ARCH-07** — GC de la dette de prose (registre INV unique, suppression code/prose mort).
4. **ARCH-03 (fond)** — OR-LLM vs driver : candidat `CHECK_EXIT`/`CHECK_CR_EXIT`/`DISPATCH` en `STEP_OR_AUTO_ADVANCE` (tout est dérivable du state désormais) — décider APRÈS le rodage live.
5. **ARCH-08 ét.2** (si friction regex récidive) — identité `--complete` via `updatedInput` (confirmé supporté par les hooks PreToolUse, JSON `permissionDecision:allow` + `updatedInput`).
6. **ARCH-10 (reste)** — port Node du monolithe. **ENH-001/002** (templates Solution Technique, agent MO).

## Gotchas (hérités du 06-07, toujours valides)

- Hook wf-auth : bloque Write des subagents dev (agent_type inconnu) et toute commande contenant un littéral `--complete PHASE:STEP` → tester via bats/sous-script.
- **Nouveau (ARCH-08)** : plus AUCUN heredoc/redirect Bash vers un artefact ne passe, pour personne — y compris en dev sur ce repo si la cible matche `wf/needs/*/<artefact>.md`.
- bats Windows lent (~3-4 s/test) : fichiers ciblés pendant le dev, full suite une fois par lot. Piste : `bats --jobs`.
- Fixes `.sh` actifs immédiatement ; fixes `.md` exigent `/reload-plugins` et n'atterrissent qu'entre needs.
- CRLF : `tr -d '\r'` avant toute comparaison shell vs jq.
